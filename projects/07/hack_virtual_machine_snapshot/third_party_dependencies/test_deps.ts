// We don't import from this file
// We import the dependencies here so we can easily cache this one file because
// it centralizes all of the imports we need in one place
export * as assert from "@test_deps/assert.ts";
export * as bdd from "@test_deps/bdd.ts";
